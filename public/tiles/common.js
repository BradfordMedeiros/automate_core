

/*

   publish(topic, value)
   getTopic(topic)
   onData (topic => ... )
  {temperature}  // find eveerything in between braces, and auto subscribe, replace

 */

setInterval(() => {
  console.log('hello world');
}, 2000);